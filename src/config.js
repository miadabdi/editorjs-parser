const defaultConfig = {
    image: {
        withCaption: "figure", // figure or img (figcaption will be used for caption)
        figureClass: "fig-img",
        imgClass: "img",
    },
};

module.exports = defaultConfig;